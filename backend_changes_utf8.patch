diff --git a/src/docs/swagger.yaml.js b/src/docs/swagger.yaml.js
index 57fc3a5..6a59e37 100644
--- a/src/docs/swagger.yaml.js
+++ b/src/docs/swagger.yaml.js
@@ -388,12 +388,6 @@
  *     responses:
  *       200: { description: Recovery proposal detail }
  *
- *               task_type: { type: string, example: "coloring" }
- *               assistant_id: { type: string, format: uuid }
- *               deadline: { type: string, format: date-time }
- *               description: { type: string }
- *     responses:
- *       201: { description: Task created }
  *
  * /api/mangaka/notifications:
  *   get:
@@ -600,6 +594,245 @@
  *     summary: Editor dashboard overview
  *     responses:
  *       200: { description: Overview stats }
+ *
+ * /api/editor/alerts:
+ *   get:
+ *     tags: [Editor]
+ *     summary: List all alerts
+ *     parameters:
+ *       - in: query
+ *         name: type
+ *         schema:
+ *           type: string
+ *           enum: [CRITICAL, HIGH, MEDIUM]
+ *     responses:
+ *       200:
+ *         description: List of alerts
+ *
+ * /api/editor/alerts/{alert_id}/resolve:
+ *   patch:
+ *     tags: [Editor]
+ *     summary: Resolve an alert
+ *     parameters:
+ *       - in: path
+ *         name: alert_id
+ *         required: true
+ *         schema:
+ *           type: string
+ *           format: uuid
+ *     responses:
+ *       200:
+ *         description: Alert resolved
+ *
+ * /api/editor/reports:
+ *   get:
+ *     tags: [Editor]
+ *     summary: List all reports
+ *     parameters:
+ *       - in: query
+ *         name: status
+ *         schema:
+ *           type: string
+ *           enum: [DRAFT, PENDING_REVIEW, APPROVED]
+ *     responses:
+ *       200:
+ *         description: List of reports
+ *   post:
+ *     tags: [Editor]
+ *     summary: Create a new report
+ *     requestBody:
+ *       required: true
+ *       content:
+ *         application/json:
+ *           schema:
+ *             type: object
+ *             required: [title, type, content]
+ *             properties:
+ *               title: { type: string }
+ *               type: { type: string, enum: [MONTHLY, CHAPTER, ALERT] }
+ *               content: { type: string }
+ *     responses:
+ *       201:
+ *         description: Report created
+ *
+ * /api/editor/reports/{report_id}:
+ *   patch:
+ *     tags: [Editor]
+ *     summary: Update a report
+ *     parameters:
+ *       - in: path
+ *         name: report_id
+ *         required: true
+ *         schema:
+ *           type: string
+ *           format: uuid
+ *     requestBody:
+ *       content:
+ *         application/json:
+ *           schema:
+ *             type: object
+ *             properties:
+ *               title: { type: string }
+ *               content: { type: string }
+ *               status: { type: string, enum: [DRAFT, PENDING_REVIEW, APPROVED] }
+ *     responses:
+ *       200:
+ *         description: Report updated
+ *
+ * /api/editor/reports/{report_id}/submit:
+ *   post:
+ *     tags: [Editor]
+ *     summary: Submit a report for review
+ *     parameters:
+ *       - in: path
+ *         name: report_id
+ *         required: true
+ *         schema:
+ *           type: string
+ *           format: uuid
+ *     responses:
+ *       200:
+ *         description: Report submitted
+ *
+ * /api/editor/proposals:
+ *   get:
+ *     tags: [Editor]
+ *     summary: List proposals
+ *     parameters:
+ *       - in: query
+ *         name: type
+ *         schema:
+ *           type: string
+ *           enum: [RECOVERY, NEW_SERIES, PUBLISH_CHAPTER, SCHEDULE_CHANGE]
+ *     responses:
+ *       200:
+ *         description: List of proposals
+ *   post:
+ *     tags: [Editor]
+ *     summary: Create a new proposal
+ *     requestBody:
+ *       required: true
+ *       content:
+ *         application/json:
+ *           schema:
+ *             type: object
+ *             required: [type, series_title, details, metadata]
+ *             properties:
+ *               type: { type: string, enum: [RECOVERY, NEW_SERIES, PUBLISH_CHAPTER, SCHEDULE_CHANGE] }
+ *               series_title: { type: string }
+ *               details: { type: string }
+ *               metadata: { type: object }
+ *     responses:
+ *       201:
+ *         description: Proposal created
+ *
+ * /api/editor/team:
+ *   get:
+ *     tags: [Editor]
+ *     summary: List team members
+ *     parameters:
+ *       - in: query
+ *         name: role
+ *         schema:
+ *           type: string
+ *           enum: [Mangaka, Assistant]
+ *     responses:
+ *       200:
+ *         description: List of team members
+ *   post:
+ *     tags: [Editor]
+ *     summary: Add team member
+ *     requestBody:
+ *       required: true
+ *       content:
+ *         application/json:
+ *           schema:
+ *             type: object
+ *             required: [name, role, series_id, workload, next_deadline]
+ *             properties:
+ *               user_id: { type: string, format: uuid }
+ *               name: { type: string }
+ *               role: { type: string, enum: [Mangaka, Assistant] }
+ *               series_id: { type: string, format: uuid }
+ *               workload: { type: integer }
+ *               next_deadline: { type: string }
+ *     responses:
+ *       201:
+ *         description: Team member added
+ *
+ * /api/editor/team/{user_id}:
+ *   patch:
+ *     tags: [Editor]
+ *     summary: Update team member
+ *     parameters:
+ *       - in: path
+ *         name: user_id
+ *         required: true
+ *         schema:
+ *           type: string
+ *           format: uuid
+ *     requestBody:
+ *       content:
+ *         application/json:
+ *           schema:
+ *             type: object
+ *             properties:
+ *               role: { type: string, enum: [Mangaka, Assistant] }
+ *               series_id: { type: string, format: uuid }
+ *               status: { type: string, enum: [ACTIVE, WARNING, AT_RISK, LATE, IDLE] }
+ *               workload: { type: integer }
+ *               next_deadline: { type: string }
+ *     responses:
+ *       200:
+ *         description: Team member updated
+ *   delete:
+ *     tags: [Editor]
+ *     summary: Delete team member
+ *     parameters:
+ *       - in: path
+ *         name: user_id
+ *         required: true
+ *         schema:
+ *           type: string
+ *           format: uuid
+ *     responses:
+ *       200:
+ *         description: Team member deleted
+ *
+ * /api/editor/team/{user_id}/nudge:
+ *   post:
+ *     tags: [Editor]
+ *     summary: Nudge team member
+ *     parameters:
+ *       - in: path
+ *         name: user_id
+ *         required: true
+ *         schema:
+ *           type: string
+ *           format: uuid
+ *     responses:
+ *       200:
+ *         description: Team member nudged
+ *
+ * /api/editor/meetings:
+ *   post:
+ *     tags: [Editor]
+ *     summary: Schedule a meeting
+ *     requestBody:
+ *       required: true
+ *       content:
+ *         application/json:
+ *           schema:
+ *             type: object
+ *             required: [user_id, meeting_date, meeting_time]
+ *             properties:
+ *               user_id: { type: string, format: uuid }
+ *               meeting_date: { type: string }
+ *               meeting_time: { type: string }
+ *               notes: { type: string }
+ *     responses:
+ *       201:
+ *         description: Meeting scheduled
  */
 
 /**
diff --git a/src/modules/admin/admin.controller.js b/src/modules/admin/admin.controller.js
index 14d99c9..f962423 100644
--- a/src/modules/admin/admin.controller.js
+++ b/src/modules/admin/admin.controller.js
@@ -111,7 +111,11 @@ const makeGetByIdHandler = (repo, pkField) => async (req, res, next) => {
 
 const makeStatusUpdateHandler = (table, pkField, pkParam) => async (req, res, next) => {
   try {
-    const { data, error } = await supabase.from(table).update({ status: req.body.status, updated_at: new Date().toISOString() }).eq(pkField, req.params[pkParam]).select().single();
+    const payload = { status: req.body.status };
+    if (table !== 'review_session') {
+      payload.updated_at = new Date().toISOString();
+    }
+    const { data, error } = await supabase.from(table).update(payload).eq(pkField, req.params[pkParam]).select().single();
     if (error) throw error;
     return sendSuccess(res, 200, data, 'Status updated');
   } catch (e) { next(e); }
@@ -193,7 +197,7 @@ const createAdminSession = async (req, res, next) => {
 };
 const updateAdminSession = async (req, res, next) => {
   try {
-    const data = await reviewSessionsRepo.update(req.params.sessionId, { ...req.body, updated_at: new Date().toISOString() });
+    const data = await reviewSessionsRepo.update(req.params.sessionId, req.body);
     return sendSuccess(res, 200, data, 'Session updated');
   } catch (e) { next(e); }
 };
diff --git a/src/modules/annotations/annotations.repository.js b/src/modules/annotations/annotations.repository.js
index 22a5a11..3f5ee2c 100644
--- a/src/modules/annotations/annotations.repository.js
+++ b/src/modules/annotations/annotations.repository.js
@@ -5,31 +5,78 @@ const SELECT = `*,
   page:page_id(page_id, page_number),
   region:region_id(region_id, x, y, width, height)`;
 
+const mapAnnotation = (ann) => {
+  if (!ann) return ann;
+  return {
+    ...ann,
+    coordinates: {
+      x: ann.x,
+      y: ann.y
+    }
+  };
+};
+
 const findAll = async ({ pageId, taskId } = {}) => {
   let query = supabase.from('annotation').select(SELECT);
   if (pageId) query = query.eq('page_id', pageId);
   if (taskId) query = query.eq('task_id', taskId);
   const { data, error } = await query;
   if (error) throw error;
-  return data;
+  return data ? data.map(mapAnnotation) : [];
 };
 
 const findById = async (annotationId) => {
   const { data, error } = await supabase.from('annotation').select(SELECT).eq('annotation_id', annotationId).maybeSingle();
   if (error) throw error;
-  return data;
+  return mapAnnotation(data);
 };
 
 const create = async (payload) => {
-  const { data, error } = await supabase.from('annotation').insert(payload).select(SELECT).single();
+  const { coordinates, ...rest } = payload;
+  const dbPayload = { ...rest };
+  if (coordinates) {
+    dbPayload.x = coordinates.x;
+    dbPayload.y = coordinates.y;
+  }
+
+  // Ensure page record exists for manuscript file (satisfies fk_annotation_page constraint)
+  if (dbPayload.page_id) {
+    const { data: page } = await supabase.from('page').select('page_id').eq('page_id', dbPayload.page_id).maybeSingle();
+    if (!page) {
+      const { data: file } = await supabase.from('manuscript_file').select('*').eq('file_id', dbPayload.page_id).maybeSingle();
+      if (file) {
+        const { data: manuscript } = await supabase.from('manuscript').select('chapter_id').eq('manuscript_id', file.manuscript_id).maybeSingle();
+        if (manuscript && manuscript.chapter_id) {
+          const { data: existingPages } = await supabase.from('page').select('page_number').eq('chapter_id', manuscript.chapter_id);
+          const page_number = (existingPages?.length || 0) + 1;
+          
+          await supabase.from('page').insert({
+            page_id: dbPayload.page_id,
+            chapter_id: manuscript.chapter_id,
+            page_number,
+            image_url: file.file_url,
+            status: 'draft'
+          });
+        }
+      }
+    }
+  }
+
+  const { data, error } = await supabase.from('annotation').insert(dbPayload).select(SELECT).single();
   if (error) throw error;
-  return data;
+  return mapAnnotation(data);
 };
 
 const update = async (annotationId, payload) => {
-  const { data, error } = await supabase.from('annotation').update(payload).eq('annotation_id', annotationId).select(SELECT).single();
+  const { coordinates, ...rest } = payload;
+  const dbPayload = { ...rest };
+  if (coordinates) {
+    dbPayload.x = coordinates.x;
+    dbPayload.y = coordinates.y;
+  }
+  const { data, error } = await supabase.from('annotation').update(dbPayload).eq('annotation_id', annotationId).select(SELECT).single();
   if (error) throw error;
-  return data;
+  return mapAnnotation(data);
 };
 
 const deleteById = async (annotationId) => {
diff --git a/src/modules/board/board.controller.js b/src/modules/board/board.controller.js
index 21909ad..5d78928 100644
--- a/src/modules/board/board.controller.js
+++ b/src/modules/board/board.controller.js
@@ -10,7 +10,14 @@ const AppError = require('../../utils/appError');
 const listProposals = async (req, res, next) => {
   try {
     const { page, limit, offset } = parsePagination(req.query);
-    const { data, error, count } = await supabase.from('review_session').select('*', { count: 'exact' }).order('created_at', { ascending: false }).range(offset, offset + limit - 1);
+    const { data, error, count } = await supabase
+      .from('review_session')
+      .select(
+        `*, series:series_id(series_id, title, cover_image_url), chapter:chapter_id(chapter_id, chapter_number, title)`,
+        { count: 'exact' }
+      )
+      .order('created_at', { ascending: false })
+      .range(offset, offset + limit - 1);
     if (error) throw error;
     return res.status(200).json({ success: true, message: 'Success', data, pagination: buildPaginationMeta(page, limit, count) });
   } catch (e) { next(e); }
@@ -18,7 +25,11 @@ const listProposals = async (req, res, next) => {
 
 const listPendingProposals = async (req, res, next) => {
   try {
-    const { data, error } = await supabase.from('review_session').select('*').in('status', ['pending', 'in_progress']).order('created_at', { ascending: false });
+    const { data, error } = await supabase
+      .from('review_session')
+      .select(`*, series:series_id(series_id, title, cover_image_url), chapter:chapter_id(chapter_id, chapter_number, title)`)
+      .in('status', ['pending', 'in_progress'])
+      .order('created_at', { ascending: false });
     if (error) throw error;
     return sendSuccess(res, 200, data, 'Success');
   } catch (e) { next(e); }
@@ -51,7 +62,11 @@ const listPendingSeries = async (req, res, next) => {
 
 const listPendingChapters = async (req, res, next) => {
   try {
-    const { data, error } = await supabase.from('chapter').select('*').in('status', ['pending_review', 'approved']).order('updated_at', { ascending: false });
+    const { data, error } = await supabase
+      .from('chapter')
+      .select('*, series:series_id(*)')
+      .in('status', ['pending_review', 'approved'])
+      .order('updated_at', { ascending: false });
     if (error) throw error;
     return sendSuccess(res, 200, data, 'Success');
   } catch (e) { next(e); }
diff --git a/src/modules/board/boardDecisions.service.js b/src/modules/board/boardDecisions.service.js
index ffde6c1..04de398 100644
--- a/src/modules/board/boardDecisions.service.js
+++ b/src/modules/board/boardDecisions.service.js
@@ -19,31 +19,145 @@ const processSessionResult = async (sessionId) => {
 
   await reviewSessionsRepo.update(sessionId, { status: 'completed', ended_at: new Date().toISOString() });
 
+  // Automatically apply decision based on dominant vote
+  let decisionStr = '';
+  const upperDec = dominantDecision?.toUpperCase() || '';
+  if (upperDec === 'APPROVE' || upperDec === 'APPROVED') decisionStr = 'approved';
+  else if (upperDec === 'REJECT' || upperDec === 'REJECTED') decisionStr = 'rejected';
+
+  if (decisionStr) {
+    const session = await reviewSessionsRepo.findById(sessionId);
+    if (session) {
+      if (session.chapter_id) {
+        await applyDecisionToChapter(session.chapter_id, decisionStr, `Automatically ${decisionStr} based on board votes.`);
+      } else if (session.series_id) {
+        await applyDecisionToSeries(session.series_id, decisionStr, `Automatically ${decisionStr} based on board votes.`);
+      }
+    }
+  }
+
   return { session_id: sessionId, total_votes: submitted.length, avg_score: Math.round(avgScore * 10) / 10, decision_count: decisionCount, dominant_decision: dominantDecision };
 };
 
 const applyDecisionToSeries = async (seriesId, status, note) => {
   const series = await seriesRepo.findById(seriesId);
   if (!series) throw new AppError('Series not found', 404);
-  const updated = await seriesRepo.update(seriesId, { status, updated_at: new Date().toISOString() });
+  const finalStatus = status;
+  const updated = await seriesRepo.update(seriesId, { status: finalStatus, updated_at: new Date().toISOString() });
 
   const { data: members } = await supabase.from('series_member').select('user_id').eq('series_id', seriesId);
   if (members?.length) {
-    await createNotifications(members.map((m) => ({ userId: m.user_id, title: `Series ${status}`, content: note || `Series decision: ${status}`, type: 'decision_result' })));
+    await createNotifications(members.map((m) => ({ userId: m.user_id, title: `Series ${finalStatus}`, content: note || `Series decision: ${finalStatus}`, type: 'decision_result' })));
   }
   return updated;
 };
 
+const syncManuscriptFilesToPages = async (manuscriptId) => {
+  const m = await manuscriptsRepo.findById(manuscriptId);
+  if (!m || !m.chapter_id) return;
+  const chapterId = m.chapter_id;
+
+  const { data: files, error: err1 } = await supabase
+    .from('manuscript_file')
+    .select('*')
+    .eq('manuscript_id', manuscriptId)
+    .order('uploaded_at');
+  if (err1) throw err1;
+  if (!files || files.length === 0) return;
+
+  const { data: currentPages, error: err2 } = await supabase
+    .from('page')
+    .select('*')
+    .eq('chapter_id', chapterId);
+  if (err2) throw err2;
+
+  const fileIds = files.map(f => f.file_id);
+
+  // Soft delete pages that are not in the new files list
+  const pagesToSoftDelete = (currentPages || []).filter(p => !fileIds.includes(p.page_id));
+  for (const p of pagesToSoftDelete) {
+    const hashNum = parseInt(p.page_id.substring(0, 8), 16) % 1000000000;
+    const finalPageNum = -1 - hashNum;
+
+    await supabase
+      .from('page')
+      .update({ 
+        status: 'deleted', 
+        page_number: finalPageNum, 
+        updated_at: new Date().toISOString() 
+      })
+      .eq('page_id', p.page_id);
+  }
+
+  // Insert or update pages
+  for (let i = 0; i < files.length; i++) {
+    const file = files[i];
+    const pageNum = i + 1;
+    const existingPage = (currentPages || []).find(p => p.page_id === file.file_id);
+    
+    if (existingPage) {
+      await supabase
+        .from('page')
+        .update({
+          chapter_id: chapterId,
+          page_number: pageNum,
+          image_url: file.file_url,
+          status: 'published',
+          updated_at: new Date().toISOString()
+        })
+        .eq('page_id', file.file_id);
+    } else {
+      await supabase
+        .from('page')
+        .insert({
+          page_id: file.file_id,
+          chapter_id: chapterId,
+          page_number: pageNum,
+          image_url: file.file_url,
+          status: 'published'
+        });
+    }
+  }
+};
+
 const applyDecisionToChapter = async (chapterId, status, note) => {
   const chapter = await chaptersRepo.findById(chapterId);
   if (!chapter) throw new AppError('Chapter not found', 404);
-  return chaptersRepo.update(chapterId, { status, updated_at: new Date().toISOString() });
+  const finalStatus = status === 'approved' ? 'published' : status;
+  const updated = await chaptersRepo.update(chapterId, { status: finalStatus, updated_at: new Date().toISOString() });
+  
+  if (finalStatus === 'published' || finalStatus === 'approved') {
+    // Automatically transition parent series to 'published'
+    if (chapter.series_id) {
+      await seriesRepo.update(chapter.series_id, { status: 'published', updated_at: new Date().toISOString() });
+    }
+
+    const { data: manuscripts } = await supabase
+      .from('manuscript')
+      .select('manuscript_id')
+      .eq('chapter_id', chapterId)
+      .order('created_at', { ascending: false })
+      .limit(1);
+      
+    if (manuscripts && manuscripts.length > 0) {
+      const manuscriptId = manuscripts[0].manuscript_id;
+      await supabase.from('manuscript').update({ status: finalStatus, updated_at: new Date().toISOString() }).eq('manuscript_id', manuscriptId);
+      await syncManuscriptFilesToPages(manuscriptId);
+    }
+  }
+  return updated;
 };
 
 const applyDecisionToManuscript = async (manuscriptId, status, note) => {
   const m = await manuscriptsRepo.findById(manuscriptId);
   if (!m) throw new AppError('Manuscript not found', 404);
-  return manuscriptsRepo.update(manuscriptId, { status, updated_at: new Date().toISOString() });
+  const finalStatus = status === 'approved' ? 'published' : status;
+  const updated = await manuscriptsRepo.update(manuscriptId, { status: finalStatus, updated_at: new Date().toISOString() });
+  
+  if (finalStatus === 'published' || finalStatus === 'approved') {
+    await syncManuscriptFilesToPages(manuscriptId);
+  }
+  return updated;
 };
 
 const getDecisionHistory = async (entityType, entityId) => {
@@ -56,4 +170,4 @@ const getDecisionHistory = async (entityType, entityId) => {
   return sessions || [];
 };
 
-module.exports = { processSessionResult, applyDecisionToSeries, applyDecisionToChapter, applyDecisionToManuscript, getDecisionHistory };
+module.exports = { processSessionResult, applyDecisionToSeries, applyDecisionToChapter, applyDecisionToManuscript, getDecisionHistory, syncManuscriptFilesToPages };
diff --git a/src/modules/chapters/chapters.repository.js b/src/modules/chapters/chapters.repository.js
index cc3de17..f36608b 100644
--- a/src/modules/chapters/chapters.repository.js
+++ b/src/modules/chapters/chapters.repository.js
@@ -3,7 +3,24 @@ const supabase = require('../../config/supabase');
 const ALLOWED_SORT = ['chapter_number', 'title', 'status', 'view_count', 'created_at'];
 
 const findAll = async ({ status, keyword, seriesId, offset, limit, sort, order }) => {
-  let query = supabase.from('chapter').select('*', { count: 'exact' });
+  let query = supabase.from('chapter').select(`
+    *,
+    series:series_id(
+      series_id,
+      title,
+      series_member(
+        role_in_series,
+        users:user_id(
+          user_id,
+          username
+        )
+      )
+    ),
+    page(
+      page_id,
+      status
+    )
+  `, { count: 'exact' });
 
   if (seriesId) query = query.eq('series_id', seriesId);
   if (status) query = query.eq('status', status);
diff --git a/src/modules/chapters/chapters.service.js b/src/modules/chapters/chapters.service.js
index 9615f11..bcb4403 100644
--- a/src/modules/chapters/chapters.service.js
+++ b/src/modules/chapters/chapters.service.js
@@ -2,7 +2,30 @@ const chaptersRepo = require('./chapters.repository');
 const seriesRepo = require('../series/series.repository');
 const AppError = require('../../utils/appError');
 
-const listChapters = async (filters) => chaptersRepo.findAll(filters);
+const listChapters = async (filters) => {
+  const result = await chaptersRepo.findAll(filters);
+  if (result && result.data) {
+    result.data = result.data.map(ch => {
+      // Calculate progress
+      const pages = ch.page || [];
+      const approvedCount = pages.filter(p => p.status === 'completed' || p.status === 'approved').length;
+      const progress = pages.length > 0 ? Math.round((approvedCount / pages.length) * 100) : 0;
+      ch.progress = progress;
+
+      // Resolve series owner
+      if (ch.series && ch.series.series_member) {
+        const ownerMember = ch.series.series_member.find(m => m.role_in_series === 'owner');
+        if (ownerMember && ownerMember.users) {
+          ch.series.owner = {
+            username: ownerMember.users.username || 'ΓÇö'
+          };
+        }
+      }
+      return ch;
+    });
+  }
+  return result;
+};
 
 const getChapterById = async (chapterId) => {
   const chapter = await chaptersRepo.findById(chapterId);
@@ -44,8 +67,8 @@ const updateChapterStatus = async (chapterId, status) => {
 
   if (status === 'published') {
     const series = await seriesRepo.findById(chapter.series_id);
-    if (!series || series.status !== 'published') {
-      throw new AppError('Cannot publish chapter: series is not published', 400);
+    if (series && series.status !== 'published') {
+      await seriesRepo.update(chapter.series_id, { status: 'published', updated_at: new Date().toISOString() });
     }
   }
 
diff --git a/src/modules/editor/editor.controller.js b/src/modules/editor/editor.controller.js
index d24b6f9..0e16f58 100644
--- a/src/modules/editor/editor.controller.js
+++ b/src/modules/editor/editor.controller.js
@@ -1,6 +1,7 @@
 const taskReviewSvc = require("./editorTaskReview.service");
 const manuscriptReviewSvc = require("./editorManuscriptReview.service");
 const dashboardSvc = require("./editorDashboard.service");
+const { syncManuscriptFilesToPages, processSessionResult, applyDecisionToSeries, applyDecisionToChapter } = require("../board/boardDecisions.service");
 const seriesRepo = require("../series/series.repository");
 const seriesMembersRepo = require("../seriesMembers/seriesMembers.repository");
 const usersRepo = require("../users/users.repository");
@@ -101,6 +102,44 @@ const seriesWorkflow = (status) => async (req, res, next) => {
   }
 };
 
+// Tantou: after reviewing, submit series to Editorial Board by creating a review_session
+const submitSeriesToBoard = async (req, res, next) => {
+  try {
+    const { seriesId } = req.params;
+    const editorId = req.user?.user_id || req.user?.id;
+
+    const series = await seriesRepo.findById(seriesId);
+    if (!series) return next(new AppError('Series not found', 404));
+    if (series.status !== 'pending_review') {
+      return next(new AppError(`Series must be in 'pending_review' status to submit to board. Current: '${series.status}'`, 400));
+    }
+
+    // Cancel any existing pending sessions for this series
+    const existing = await reviewSessionsRepo.findAll({ seriesId });
+    for (const s of existing) {
+      if (['pending', 'in_progress'].includes(s.status)) {
+        await reviewSessionsRepo.update(s.session_id, { status: 'cancelled' });
+      }
+    }
+
+    // Create review session for the Board
+    const session = await reviewSessionsRepo.create({
+      series_id: seriesId,
+      chapter_id: null,
+      status: 'pending',
+      created_by_user_id: editorId,
+      name: `Series Review - ${series.title}`,
+      started_at: new Date().toISOString(),
+      created_at: new Date().toISOString(),
+    });
+
+    return sendSuccess(res, 201, session, 'Series submitted to Editorial Board successfully');
+  } catch (e) {
+    next(e);
+  }
+};
+
+
 // --- Team Management ---
 const listMembers = async (req, res, next) => {
   try {
@@ -123,7 +162,7 @@ const addMember = async (req, res, next) => {
     const data = await seriesMembersRepo.create({
       series_id: req.params.seriesId,
       user_id: req.body.user_id,
-      role_in_series: req.body.role_in_series,
+      role_in_series: req.body.role_in_series || req.body.role,
     });
     return sendSuccess(res, 201, data, "Member added");
   } catch (e) {
@@ -134,7 +173,7 @@ const addMember = async (req, res, next) => {
 const updateMember = async (req, res, next) => {
   try {
     const data = await seriesMembersRepo.update(req.params.seriesMemberId, {
-      role_in_series: req.body.role_in_series,
+      role_in_series: req.body.role_in_series || req.body.role,
     });
     return sendSuccess(res, 200, data, "Member updated");
   } catch (e) {
@@ -406,6 +445,11 @@ const manuscriptWorkflow = (action) => async (req, res, next) => {
       action,
       req.body,
     );
+    
+    if (action === 'approve' || (action === 'override-status' && (req.body.status === 'approved' || req.body.status === 'published'))) {
+      await syncManuscriptFilesToPages(req.params.manuscriptId);
+    }
+    
     return sendSuccess(
       res,
       200,
@@ -423,6 +467,9 @@ const bulkApproveManuscripts = async (req, res, next) => {
       req.body.manuscript_ids,
       "approved",
     );
+    for (const id of req.body.manuscript_ids) {
+      await syncManuscriptFilesToPages(id);
+    }
     return sendSuccess(res, 200, null, "Manuscripts approved");
   } catch (e) {
     next(e);
@@ -447,6 +494,11 @@ const bulkUpdateManuscriptStatus = async (req, res, next) => {
       req.body.manuscript_ids,
       req.body.status,
     );
+    if (req.body.status === 'approved' || req.body.status === 'published') {
+      for (const id of req.body.manuscript_ids) {
+        await syncManuscriptFilesToPages(id);
+      }
+    }
     return sendSuccess(res, 200, null, "Manuscripts updated");
   } catch (e) {
     next(e);
@@ -512,7 +564,7 @@ const createAnnotation = async (req, res, next) => {
     const data = await annotationsRepo.create({
       ...req.body,
       page_id: req.params.pageId,
-      created_by: req.user.user_id,
+      user_id: req.user.user_id,
     });
     return sendSuccess(res, 201, data, "Annotation created");
   } catch (e) {
@@ -563,12 +615,34 @@ const listTaskFeedbacks = async (req, res, next) => {
   }
 };
 
+const listAllFeedbacks = async (req, res, next) => {
+  try {
+    const data = await pageTaskFeedbacksRepo.findAll();
+    return sendSuccess(res, 200, data, "Success");
+  } catch (e) {
+    next(e);
+  }
+};
+
 const createFeedback = async (req, res, next) => {
   try {
+    const { taskId } = req.params;
+    const { data: latestSub, error: subErr } = await supabase
+      .from('page_submission')
+      .select('submission_id')
+      .eq('task_id', taskId)
+      .order('submitted_at', { ascending: false })
+      .limit(1)
+      .maybeSingle();
+    if (subErr) throw subErr;
+    if (!latestSub) {
+      throw new AppError('No submission found for this task. Feedback can only be created for tasks with submissions.', 400);
+    }
     const data = await pageTaskFeedbacksRepo.create({
-      ...req.body,
-      task_id: req.params.taskId,
+      submission_id: latestSub.submission_id,
       mangaka_id: req.user.user_id,
+      assistant_id: req.body.assistant_id || null,
+      content: req.body.content || req.body.feedback_content,
     });
     return sendSuccess(res, 201, data, "Feedback created");
   } catch (e) {
@@ -658,6 +732,26 @@ const getReviewSessionDetail = async (req, res, next) => {
 
 const createReviewSession = async (req, res, next) => {
   try {
+    const { series_id, chapter_id } = req.body;
+
+    if (chapter_id) {
+      // Cancel any existing pending or in_progress sessions for this chapter
+      const existing = await reviewSessionsRepo.findAll({ chapterId: chapter_id });
+      for (const s of existing) {
+        if (['pending', 'in_progress'].includes(s.status)) {
+          await reviewSessionsRepo.update(s.session_id, { status: 'cancelled' });
+        }
+      }
+    } else if (series_id) {
+      // Cancel any existing pending or in_progress sessions for this series (where chapter_id is null)
+      const existing = await reviewSessionsRepo.findAll({ seriesId: series_id });
+      for (const s of existing) {
+        if (!s.chapter_id && ['pending', 'in_progress'].includes(s.status)) {
+          await reviewSessionsRepo.update(s.session_id, { status: 'cancelled' });
+        }
+      }
+    }
+
     const data = await reviewSessionsRepo.create({
       ...req.body,
       created_by_user_id: req.user.user_id,
@@ -673,7 +767,6 @@ const updateReviewSession = async (req, res, next) => {
   try {
     const data = await reviewSessionsRepo.update(req.params.sessionId, {
       ...req.body,
-      updated_at: new Date().toISOString(),
     });
     return sendSuccess(res, 200, data, "Session updated");
   } catch (e) {
@@ -692,12 +785,37 @@ const reviewSessionWorkflow = (action) => async (req, res, next) => {
     };
     const updates = {
       status: WORKFLOW[action],
-      updated_at: new Date().toISOString(),
     };
     if (action === "start") updates.started_at = new Date().toISOString();
     if (["complete", "finish"].includes(action))
       updates.ended_at = new Date().toISOString();
     const data = await reviewSessionsRepo.update(req.params.sessionId, updates);
+
+    // If completing or finishing the session, automatically process the result and apply decision
+    if (["complete", "finish"].includes(action)) {
+      try {
+        const result = await processSessionResult(req.params.sessionId);
+        
+        let decisionStr = '';
+        const upperDec = result.dominant_decision?.toUpperCase() || '';
+        if (upperDec === 'APPROVE' || upperDec === 'APPROVED') decisionStr = 'approved';
+        else if (upperDec === 'REJECT' || upperDec === 'REJECTED') decisionStr = 'rejected';
+        
+        if (decisionStr) {
+          const session = await reviewSessionsRepo.findById(req.params.sessionId);
+          if (session) {
+            if (session.chapter_id) {
+              await applyDecisionToChapter(session.chapter_id, decisionStr, `Automatically ${decisionStr} based on board votes.`);
+            } else if (session.series_id) {
+              await applyDecisionToSeries(session.series_id, decisionStr, `Automatically ${decisionStr} based on board votes.`);
+            }
+          }
+        }
+      } catch (err) {
+        console.error("Error processing session result or applying decisions automatically:", err);
+      }
+    }
+
     return sendSuccess(res, 200, data, `Session ${action} successful`);
   } catch (e) {
     next(e);
@@ -707,7 +825,7 @@ const reviewSessionWorkflow = (action) => async (req, res, next) => {
 // Proposals (map to review_session)
 const createProposal = async (req, res, next) => {
   try {
-    const data = await editorProposalsSvc.createProposal(req.body);
+    const data = await editorProposalsSvc.createProposal(req.body, req.user.user_id);
     return sendSuccess(res, 201, data, "Proposal created");
   } catch (e) {
     next(e);
@@ -996,7 +1114,9 @@ module.exports = {
   createMeeting,
   listNotifications,
   getUnreadNotifications,
-  markNotificationRead,
   markAllRead,
+  markNotificationRead,
   deleteNotification,
+  listAllFeedbacks,
+  submitSeriesToBoard,
 };
diff --git a/src/modules/editor/editor.routes.js b/src/modules/editor/editor.routes.js
index 738ae6b..797150a 100644
--- a/src/modules/editor/editor.routes.js
+++ b/src/modules/editor/editor.routes.js
@@ -21,6 +21,8 @@ router.patch("/series/:seriesId/status", ctrl.updateSeriesStatus);
 router.patch("/series/:seriesId/hide", ctrl.seriesWorkflow("hidden"));
 router.patch("/series/:seriesId/archive", ctrl.seriesWorkflow("archived"));
 router.patch("/series/:seriesId/republish", ctrl.seriesWorkflow("published"));
+// Tantou submits a pending_review series to the Editorial Board (creates review_session)
+router.post("/series/:seriesId/submit-to-board", ctrl.submitSeriesToBoard);
 
 // Team
 router.get("/series/:seriesId/members", ctrl.listMembers);
@@ -121,6 +123,7 @@ router.patch("/annotations/:annotationId/status", ctrl.updateAnnotationStatus);
 router.delete("/annotations/:annotationId", ctrl.deleteAnnotation);
 
 // Feedbacks
+router.get("/feedbacks", ctrl.listAllFeedbacks);
 router.get("/page-tasks/:taskId/feedbacks", ctrl.listTaskFeedbacks);
 router.post("/page-tasks/:taskId/feedbacks", ctrl.createFeedback);
 router.patch("/page-task-feedbacks/:feedbackId", ctrl.updateFeedback);
diff --git a/src/modules/editor/editorDashboard.service.js b/src/modules/editor/editorDashboard.service.js
index e3cdfea..8b1ddb8 100644
--- a/src/modules/editor/editorDashboard.service.js
+++ b/src/modules/editor/editorDashboard.service.js
@@ -1,18 +1,77 @@
 const supabase = require('../../config/supabase');
+const editorMockStore = require('./editorMockStore');
 
 const getOverview = async () => {
-  const now = new Date().toISOString();
-  const [{ count: totalSeries }, { count: totalChapters }, { count: totalPages }, { data: tasks }] = await Promise.all([
-    supabase.from('series').select('*', { count: 'exact', head: true }),
-    supabase.from('chapter').select('*', { count: 'exact', head: true }),
-    supabase.from('page').select('*', { count: 'exact', head: true }),
+  const now = new Date();
+  const startOfMonth = new Date();
+  startOfMonth.setDate(1);
+  startOfMonth.setHours(0,0,0,0);
+
+  const nextWeek = new Date();
+  nextWeek.setDate(nextWeek.getDate() + 7);
+
+  const [
+    { count: totalSeries },
+    { count: publishingCount },
+    { count: pausedCount },
+    { count: pendingManuscripts },
+    { count: needRevision },
+    { count: approvedThisMonth },
+    { data: tasks },
+    { count: newResubmissions },
+    { data: notifications }
+  ] = await Promise.all([
+    supabase.from('series').select('*', { count: 'exact', head: true }).neq('status', 'deleted'),
+    supabase.from('series').select('*', { count: 'exact', head: true }).eq('status', 'published'),
+    supabase.from('series').select('*', { count: 'exact', head: true }).in('status', ['archived', 'hidden']),
+    supabase.from('manuscript').select('*', { count: 'exact', head: true }).in('status', ['submitted', 'in_review']),
+    supabase.from('manuscript').select('*', { count: 'exact', head: true }).eq('status', 'needs_revision'),
+    supabase.from('manuscript').select('*', { count: 'exact', head: true }).eq('status', 'approved').gte('updated_at', startOfMonth.toISOString()),
     supabase.from('page_task').select('task_id, status, deadline'),
+    supabase.from('page_submission').select('*', { count: 'exact', head: true }).eq('submission_status', 'pending'),
+    supabase.from('notification').select('*').order('created_at', { ascending: false }).limit(6)
   ]);
 
-  const byStatus = (tasks || []).reduce((acc, t) => { acc[t.status] = (acc[t.status] ?? 0) + 1; return acc; }, {});
-  const overdue = (tasks || []).filter((t) => t.deadline && new Date(t.deadline) < new Date() && !['completed', 'cancelled', 'rejected'].includes(t.status)).length;
+  const alerts = editorMockStore.getAlerts();
+  const criticalOrHighAlerts = alerts.filter(a => a.type === 'CRITICAL' || a.type === 'HIGH');
+  const atRiskCount = criticalOrHighAlerts.length;
+  const atRiskSeriesName = alerts.find(a => a.type === 'CRITICAL')?.series_title || 'ΓÇö';
+  
+  const deadlineThisWeek = (tasks || []).filter(t => 
+    t.deadline && 
+    new Date(t.deadline) >= now && 
+    new Date(t.deadline) <= nextWeek && 
+    !['completed', 'cancelled', 'rejected'].includes(t.status)
+  ).length;
+
+  const overdueCount = (tasks || []).filter(t => 
+    t.deadline && 
+    new Date(t.deadline) < now && 
+    !['completed', 'cancelled', 'rejected'].includes(t.status)
+  ).length;
+
+  const reportsToSend = editorMockStore.getReports({ status: 'DRAFT' }).length;
 
-  return { total_series: totalSeries ?? 0, total_chapters: totalChapters ?? 0, total_pages: totalPages ?? 0, total_tasks: tasks?.length ?? 0, ...byStatus, overdue_tasks: overdue };
+  return {
+    managing_series: totalSeries ?? 0,
+    publishing_count: publishingCount ?? 0,
+    at_risk_count: atRiskCount,
+    paused_count: pausedCount ?? 0,
+    pending_review: pendingManuscripts ?? 0,
+    deadline_this_week: deadlineThisWeek,
+    need_revision: needRevision ?? 0,
+    approved_this_month: approvedThisMonth ?? 0,
+    change_from_last_month: 0,
+    overdue_count: overdueCount,
+    at_risk_series_count: atRiskCount,
+    at_risk_series_name: atRiskSeriesName,
+    at_risk_series_ranking: atRiskCount > 0 ? 15 : 0,
+    chapters_to_review: pendingManuscripts ?? 0,
+    new_resubmissions: newResubmissions ?? 0,
+    risk_alerts: atRiskCount,
+    reports_to_send: reportsToSend,
+    recent_notifications: notifications || []
+  };
 };
 
 const getSeriesProgress = async (seriesId) => {
diff --git a/src/modules/editor/editorManuscriptReview.service.js b/src/modules/editor/editorManuscriptReview.service.js
index d031189..e1931ca 100644
--- a/src/modules/editor/editorManuscriptReview.service.js
+++ b/src/modules/editor/editorManuscriptReview.service.js
@@ -3,8 +3,13 @@ const manuscriptsRepo = require('../manuscripts/manuscripts.repository');
 const { createNotification, createNotifications } = require('../../utils/notification.helper');
 const AppError = require('../../utils/appError');
 
+const SELECT_MANUSCRIPT_WITH_RELATIONS = `*,
+  series:series_id(series_id, title),
+  chapter:chapter_id(chapter_id, chapter_number, title),
+  mangaka:users!fk_manuscript_mangaka(user_id, username, name)`;
+
 const listManuscripts = async (filters) => {
-  let query = supabase.from('manuscript').select('*', { count: 'exact' });
+  let query = supabase.from('manuscript').select(SELECT_MANUSCRIPT_WITH_RELATIONS, { count: 'exact' });
   if (filters.status) query = query.eq('status', filters.status);
   if (filters.seriesId) query = query.eq('series_id', filters.seriesId);
   if (filters.chapterId) query = query.eq('chapter_id', filters.chapterId);
diff --git a/src/modules/editor/editorProposals.service.js b/src/modules/editor/editorProposals.service.js
index 037b198..db01c91 100644
--- a/src/modules/editor/editorProposals.service.js
+++ b/src/modules/editor/editorProposals.service.js
@@ -1,8 +1,131 @@
-const editorMockStore = require("./editorMockStore");
+const supabase = require('../../config/supabase');
 
-const listProposals = async ({ type } = {}) =>
-  editorMockStore.getProposals({ type });
+const listProposals = async ({ type } = {}) => {
+  const q = supabase
+    .from('recovery_proposal')
+    .select(`
+      proposal_id,
+      title,
+      description,
+      status,
+      created_at,
+      created_by_user_id,
+      series:series_id(series_id, title)
+    `)
+    .order('created_at', { ascending: false });
+  
+  const { data, error } = await q;
+  if (error) throw error;
 
-const createProposal = async (payload) => editorMockStore.addProposal(payload);
+  const results = (data || []).map(item => {
+    let pType = 'RECOVERY';
+    let details = item.description || '';
+    let metadata = {};
+
+    // Check if type is encoded in description
+    if (details.startsWith('[TYPE:')) {
+      const closingBracket = details.indexOf(']');
+      if (closingBracket > -1) {
+        pType = details.substring(6, closingBracket);
+        details = details.substring(closingBracket + 1).trim();
+      }
+    }
+
+    // Check if metadata is encoded in description
+    const metadataIndex = details.indexOf('| Metadata:');
+    if (metadataIndex > -1) {
+      try {
+        metadata = JSON.parse(details.substring(metadataIndex + 11).trim());
+      } catch (e) {
+        // ignore
+      }
+      details = details.substring(0, metadataIndex).trim();
+    }
+
+    return {
+      proposal_id: item.proposal_id,
+      type: pType,
+      series_title: item.series?.title || item.title || 'Unknown Series',
+      details: details || item.description,
+      status: (item.status || 'pending').toUpperCase(),
+      created_at: item.created_at,
+      metadata: Object.keys(metadata).length > 0 ? metadata : {
+        reason: details,
+        plan: details
+      },
+      rejection_reason: null
+    };
+  });
+
+  if (type) {
+    return results.filter(p => p.type === type);
+  }
+  return results;
+};
+
+const createProposal = async (payload, userId) => {
+  const { type, series_title, details, metadata } = payload;
+  
+  // Find or map series_id
+  let seriesId = null;
+  const { data: matched } = await supabase
+    .from('series')
+    .select('series_id')
+    .ilike('title', `%${series_title || ''}%`)
+    .limit(1)
+    .maybeSingle();
+  
+  if (matched) {
+    seriesId = matched.series_id;
+  } else {
+    const { data: fallbackSeries } = await supabase
+      .from('series')
+      .select('series_id')
+      .limit(1)
+      .maybeSingle();
+    seriesId = fallbackSeries?.series_id || null;
+  }
+
+  if (!seriesId) {
+    throw new Error("No series found in database to associate proposal with");
+  }
+
+  let creatorId = userId;
+  if (!creatorId) {
+    const { data: firstUser } = await supabase
+      .from('users')
+      .select('user_id')
+      .limit(1)
+      .maybeSingle();
+    creatorId = firstUser?.user_id;
+  }
+
+  const encodedDescription = `[TYPE:${type}] ${details || ''} | Metadata: ${JSON.stringify(metadata || {})}`;
+
+  const { data, error } = await supabase
+    .from('recovery_proposal')
+    .insert({
+      series_id: seriesId,
+      created_by_user_id: creatorId,
+      title: series_title || 'Untitled Proposal',
+      description: encodedDescription,
+      status: 'pending'
+    })
+    .select()
+    .single();
+
+  if (error) throw error;
+  
+  return {
+    proposal_id: data.proposal_id,
+    type,
+    series_title,
+    details,
+    status: 'PENDING',
+    created_at: data.created_at,
+    metadata,
+    rejection_reason: null
+  };
+};
 
 module.exports = { listProposals, createProposal };
diff --git a/src/modules/editor/editorTaskReview.service.js b/src/modules/editor/editorTaskReview.service.js
index b717de7..c287259 100644
--- a/src/modules/editor/editorTaskReview.service.js
+++ b/src/modules/editor/editorTaskReview.service.js
@@ -4,32 +4,58 @@ const pageTaskFeedbacksRepo = require('../pageTaskFeedbacks/pageTaskFeedbacks.re
 const { createNotification, createNotifications } = require('../../utils/notification.helper');
 const AppError = require('../../utils/appError');
 
-const TASK_SELECT = `*, page:page_id(page_id,page_number,chapter:chapter_id(chapter_id,title,series:series_id(series_id,title))), users!fk_page_task_assistant(user_id,username,email), users!fk_page_task_assigned_by(user_id,username,email)`;
+const TASK_SELECT = `*, page:page_id(page_id,page_number,chapter:chapter_id(chapter_id,title,series:series_id(series_id,title))), assistant:users!fk_page_task_assistant(user_id,username,email), assigned_by:users!fk_page_task_assigned_by(user_id,username,email)`;
 
 const listReviewTasks = async (filters) => {
   let query = supabase.from('page_task').select(TASK_SELECT, { count: 'exact' });
   if (filters.status) query = query.eq('status', filters.status);
   if (filters.assistantId) query = query.eq('assistant_id', filters.assistantId);
-  if (filters.chapterId) query = query.eq('page.chapter_id', filters.chapterId);
-  if (filters.seriesId) query = query.eq('page.chapter.series_id', filters.seriesId);
+  
+  if (filters.chapterId || filters.seriesId) {
+    let pageQuery = supabase.from('page').select('page_id, chapter!inner(chapter_id, series_id)');
+    if (filters.chapterId) pageQuery = pageQuery.eq('chapter_id', filters.chapterId);
+    if (filters.seriesId) pageQuery = pageQuery.eq('chapter.series_id', filters.seriesId);
+    
+    const { data: pages, error: pageErr } = await pageQuery;
+    if (pageErr) throw pageErr;
+    
+    const pageIds = (pages || []).map(p => p.page_id);
+    if (pageIds.length > 0) {
+      query = query.in('page_id', pageIds);
+    } else {
+      return { data: [], total: 0 };
+    }
+  }
+
   query = query.order('created_at', { ascending: false }).range(filters.offset, filters.offset + filters.limit - 1);
   const { data, error, count } = await query;
   if (error) throw error;
-  return { data, total: count };
+
+  const mappedData = (data || []).map(task => ({
+    ...task,
+    chapter: task.page?.chapter,
+    series: task.page?.chapter?.series,
+  }));
+
+  return { data: mappedData, total: count };
 };
 
 const getTaskById = async (taskId) => {
   const { data, error } = await supabase.from('page_task').select(TASK_SELECT).eq('task_id', taskId).maybeSingle();
   if (error) throw error;
   if (!data) throw new AppError('Task not found', 404);
-  return data;
+  return {
+    ...data,
+    chapter: data.page?.chapter,
+    series: data.page?.chapter?.series,
+  };
 };
 
 const getTaskDetail = async (taskId) => {
   const task = await getTaskById(taskId);
-  const [{ data: annotations }, { data: feedbacks }] = await Promise.all([
+  const [{ data: annotations }, feedbacks] = await Promise.all([
     supabase.from('annotation').select('*').eq('page_id', task.page_id),
-    supabase.from('page_task_feedback').select('*').eq('task_id', taskId).order('created_at'),
+    pageTaskFeedbacksRepo.findByTaskId(taskId),
   ]);
   return { ...task, annotations: annotations || [], feedbacks: feedbacks || [] };
 };
@@ -55,7 +81,22 @@ const performWorkflow = async (taskId, editorId, action, payload) => {
       throw new AppError(`Cannot request revision from status '${task.status}'`, 400);
     await pageTasksRepo.update(taskId, { status: 'needs_revision', updated_at: new Date().toISOString() });
     if (payload.content) {
-      await pageTaskFeedbacksRepo.create({ task_id: taskId, mangaka_id: editorId, assistant_id: task.assistant_id, feedback_content: payload.content, feedback_type: 'revision_request', status: 'pending' });
+      const { data: latestSub, error: subErr } = await supabase
+        .from('page_submission')
+        .select('submission_id')
+        .eq('task_id', taskId)
+        .order('submitted_at', { ascending: false })
+        .limit(1)
+        .maybeSingle();
+      if (subErr) throw subErr;
+      if (!latestSub) throw new AppError('No submission found for this task to revise', 400);
+
+      await pageTaskFeedbacksRepo.create({
+        submission_id: latestSub.submission_id,
+        mangaka_id: editorId,
+        assistant_id: task.assistant_id,
+        content: payload.content
+      });
     }
     if (task.assistant_id) await createNotification(task.assistant_id, 'Revision requested', payload.content || 'Please revise your submission.', 'revision_request');
     return;
@@ -82,7 +123,25 @@ const bulkUpdateTasks = async (taskIds, status, editorId, content) => {
 
   if (['rejected', 'needs_revision'].includes(status) && content) {
     const { data: tasks } = await supabase.from('page_task').select('task_id, assistant_id').in('task_id', taskIds);
-    const feedbacks = (tasks || []).map((t) => ({ task_id: t.task_id, mangaka_id: editorId, assistant_id: t.assistant_id, feedback_content: content, feedback_type: 'bulk_review', status: 'pending' }));
+    
+    const feedbacks = [];
+    for (const t of (tasks || [])) {
+      const { data: latestSub } = await supabase
+        .from('page_submission')
+        .select('submission_id')
+        .eq('task_id', t.task_id)
+        .order('submitted_at', { ascending: false })
+        .limit(1)
+        .maybeSingle();
+      if (latestSub) {
+        feedbacks.push({
+          submission_id: latestSub.submission_id,
+          mangaka_id: editorId,
+          assistant_id: t.assistant_id,
+          content: content
+        });
+      }
+    }
     if (feedbacks.length) await supabase.from('page_task_feedback').insert(feedbacks);
 
     const notifs = (tasks || []).filter((t) => t.assistant_id).map((t) => ({ userId: t.assistant_id, title: 'Tasks updated', content: content || `Tasks have been ${status}.`, type: `task_${status}` }));
diff --git a/src/modules/editor/editorTeam.service.js b/src/modules/editor/editorTeam.service.js
index aa0cdf6..b720cf2 100644
--- a/src/modules/editor/editorTeam.service.js
+++ b/src/modules/editor/editorTeam.service.js
@@ -1,6 +1,46 @@
-const editorMockStore = require("./editorMockStore");
+const supabase = require('../../config/supabase');
+const { randomUUID } = require('crypto');
 
-const listTeam = async ({ role } = {}) => editorMockStore.getTeam({ role });
+const listTeam = async ({ role } = {}) => {
+  const { data, error } = await supabase
+    .from('series_member')
+    .select(`
+      series_member_id,
+      series_id,
+      user_id,
+      role_in_series,
+      user:user_id!fk_series_member_user(user_id, name, username, avatar_url, role),
+      series:series_id!fk_series_member_series(series_id, title)
+    `);
+  if (error) throw error;
+
+  const team = (data || []).map(m => {
+    let mappedRole = 'Assistant';
+    if (m.role_in_series && m.role_in_series.toLowerCase() === 'mangaka') {
+      mappedRole = 'Mangaka';
+    } else if (m.user?.role && m.user.role.toLowerCase() === 'mangaka') {
+      mappedRole = 'Mangaka';
+    }
+
+    return {
+      user_id: m.user_id,
+      name: m.user?.name || m.user?.username || 'Unknown Member',
+      role: mappedRole,
+      series_title: m.series?.title || 'Unknown Series',
+      series_id: m.series_id,
+      status: 'ACTIVE',
+      workload: 50,
+      next_deadline: 'ΓÇö',
+      avatar_url: m.user?.avatar_url || null,
+      performance_score: 92
+    };
+  });
+
+  if (role) {
+    return team.filter(t => t.role.toLowerCase() === role.toLowerCase());
+  }
+  return team;
+};
 
 const createTeamMember = async ({
   user_id,
@@ -9,27 +49,132 @@ const createTeamMember = async ({
   series_id,
   workload,
   next_deadline,
-}) =>
-  editorMockStore.addTeamMember({
-    user_id,
+}) => {
+  let userId = user_id;
+  if (!userId) {
+    const { data: foundUser } = await supabase
+      .from('users')
+      .select('user_id')
+      .ilike('name', `%${name}%`)
+      .limit(1)
+      .maybeSingle();
+    
+    if (foundUser) {
+      userId = foundUser.user_id;
+    } else {
+      const newUsername = `user_${Date.now()}`;
+      const { data: createdUser, error: uErr } = await supabase
+        .from('users')
+        .insert({
+          username: newUsername,
+          email: `${newUsername}@example.com`,
+          name: name,
+          role: role.toLowerCase() === 'mangaka' ? 'mangaka' : 'assistant'
+        })
+        .select()
+        .single();
+      if (uErr) throw uErr;
+      userId = createdUser.user_id;
+    }
+  }
+
+  const { data: already } = await supabase
+    .from('series_member')
+    .select('*')
+    .eq('series_id', series_id)
+    .eq('user_id', userId)
+    .maybeSingle();
+  
+  if (already) {
+    await supabase
+      .from('series_member')
+      .update({ role_in_series: role })
+      .eq('series_member_id', already.series_member_id);
+  } else {
+    await supabase
+      .from('series_member')
+      .insert({
+        series_id,
+        user_id: userId,
+        role_in_series: role
+      });
+  }
+
+  return {
+    user_id: userId,
     name,
     role,
     series_id,
-    workload,
-    next_deadline,
-  });
+    status: 'ACTIVE',
+    workload: workload || 50,
+    next_deadline: next_deadline || 'ΓÇö'
+  };
+};
 
-const updateTeamMember = async (userId, payload) =>
-  editorMockStore.updateTeamMember(userId, payload);
+const updateTeamMember = async (userId, payload) => {
+  const { data: member } = await supabase
+    .from('series_member')
+    .select('*')
+    .eq('user_id', userId)
+    .limit(1)
+    .maybeSingle();
+  
+  if (member) {
+    const updates = {};
+    if (payload.role !== undefined) updates.role_in_series = payload.role;
+    if (payload.series_id !== undefined) updates.series_id = payload.series_id;
+    
+    await supabase
+      .from('series_member')
+      .update(updates)
+      .eq('series_member_id', member.series_member_id);
+    
+    return {
+      user_id: userId,
+      role: payload.role || member.role_in_series,
+      series_id: payload.series_id || member.series_id,
+      status: payload.status || 'ACTIVE',
+      workload: payload.workload || 50,
+      next_deadline: payload.next_deadline || 'ΓÇö'
+    };
+  }
+  return null;
+};
 
-const deleteTeamMember = async (userId) =>
-  editorMockStore.removeTeamMember(userId);
+const deleteTeamMember = async (userId) => {
+  const { error } = await supabase
+    .from('series_member')
+    .delete()
+    .eq('user_id', userId);
+  if (error) throw error;
+  return true;
+};
 
-const nudgeTeamMember = async (userId) =>
-  editorMockStore.nudgeTeamMember(userId);
+const nudgeTeamMember = async (userId) => {
+  const { data: user } = await supabase
+    .from('users')
+    .select('*')
+    .eq('user_id', userId)
+    .maybeSingle();
+  
+  if (user) {
+    const { createNotification } = require('../../utils/notification.helper');
+    await createNotification(userId, 'Deadline nudge', 'Please pay attention to your next deadline.', 'nudge');
+    return { user_id: userId, nudged_at: new Date().toISOString() };
+  }
+  return null;
+};
 
-const createMeeting = async ({ user_id, meeting_date, meeting_time, notes }) =>
-  editorMockStore.addMeeting({ user_id, meeting_date, meeting_time, notes });
+const createMeeting = async ({ user_id, meeting_date, meeting_time, notes }) => {
+  return {
+    meeting_id: randomUUID(),
+    user_id,
+    meeting_date,
+    meeting_time,
+    notes: notes || null,
+    created_at: new Date().toISOString(),
+  };
+};
 
 module.exports = {
   listTeam,
diff --git a/src/modules/mangaka/mangakaAnnotations.controller.js b/src/modules/mangaka/mangakaAnnotations.controller.js
index bdc1aea..4c92b7f 100644
--- a/src/modules/mangaka/mangakaAnnotations.controller.js
+++ b/src/modules/mangaka/mangakaAnnotations.controller.js
@@ -28,7 +28,7 @@ const getAnnotationById = async (req, res, next) => {
 
 const createAnnotation = async (req, res, next) => {
   try {
-    const data = await annotationsRepo.create({ ...req.body, page_id: req.params.pageId, created_by: req.user.user_id });
+    const data = await annotationsRepo.create({ ...req.body, page_id: req.params.pageId, user_id: req.user.user_id });
     return sendSuccess(res, 201, data, 'Annotation created');
   } catch (e) { next(e); }
 };
diff --git a/src/modules/mangaka/mangakaChapters.routes.js b/src/modules/mangaka/mangakaChapters.routes.js
index 783fa55..7c14fa6 100644
--- a/src/modules/mangaka/mangakaChapters.routes.js
+++ b/src/modules/mangaka/mangakaChapters.routes.js
@@ -9,8 +9,8 @@ const uuid = z.string().uuid();
 const seriesP = z.object({ seriesId: uuid });
 const chapterP = z.object({ seriesId: uuid, chapterId: uuid });
 
-const createSchema = z.object({ params: seriesP, body: z.object({ title: z.string().min(1).max(255), description: z.string().optional(), cover_image_url: z.string().url().optional(), chapter_number: z.number().int().positive().optional() }) });
-const updateSchema = z.object({ params: chapterP, body: z.object({ title: z.string().min(1).max(255).optional(), description: z.string().optional(), cover_image_url: z.string().url().optional() }) });
+const createSchema = z.object({ params: seriesP, body: z.object({ title: z.string().min(1).max(255), description: z.string().optional(), cover_image_url: z.string().url().nullable().optional(), thumbnail_image_url: z.string().url().nullable().optional(), chapter_number: z.number().int().positive().optional() }) });
+const updateSchema = z.object({ params: chapterP, body: z.object({ title: z.string().min(1).max(255).optional(), description: z.string().optional(), cover_image_url: z.string().url().nullable().optional(), thumbnail_image_url: z.string().url().nullable().optional() }) });
 const reorderSchema = z.object({ params: chapterP, body: z.object({ chapter_number: z.number().int().positive() }) });
 const copySchema = z.object({ params: seriesP, body: z.object({ source_chapter_id: uuid, title: z.string().min(1).max(255).optional() }) });
 
diff --git a/src/modules/mangaka/mangakaChapters.service.js b/src/modules/mangaka/mangakaChapters.service.js
index 37fb832..b067bca 100644
--- a/src/modules/mangaka/mangakaChapters.service.js
+++ b/src/modules/mangaka/mangakaChapters.service.js
@@ -1,5 +1,6 @@
 const supabase = require('../../config/supabase');
 const chaptersRepo = require('../chapters/chapters.repository');
+const seriesRepo = require('../series/series.repository');
 const AppError = require('../../utils/appError');
 
 const WORKFLOW = {
@@ -50,12 +51,33 @@ const createChapter = async (seriesId, payload) => {
       .maybeSingle();
     chapterNumber = (data?.chapter_number ?? 0) + 1;
   }
-  return chaptersRepo.create({ ...payload, series_id: seriesId, chapter_number: chapterNumber, status: payload.status || 'draft' });
+
+  const dbPayload = {
+    series_id: seriesId,
+    chapter_number: chapterNumber,
+    title: payload.title,
+    thumbnail_image_url: payload.thumbnail_image_url || payload.cover_image_url || null,
+    status: payload.status || 'draft'
+  };
+
+  return chaptersRepo.create(dbPayload);
 };
 
 const updateChapter = async (chapterId, seriesId, payload) => {
   await getChapterById(chapterId, seriesId);
-  return chaptersRepo.update(chapterId, { ...payload, updated_at: new Date().toISOString() });
+
+  const dbPayload = {
+    updated_at: new Date().toISOString()
+  };
+  if (payload.title !== undefined) dbPayload.title = payload.title;
+  if (payload.chapter_number !== undefined) dbPayload.chapter_number = payload.chapter_number;
+  if (payload.thumbnail_image_url !== undefined) {
+    dbPayload.thumbnail_image_url = payload.thumbnail_image_url;
+  } else if (payload.cover_image_url !== undefined) {
+    dbPayload.thumbnail_image_url = payload.cover_image_url;
+  }
+
+  return chaptersRepo.update(chapterId, dbPayload);
 };
 
 const reorderChapter = async (chapterId, seriesId, newNumber) => {
@@ -69,6 +91,14 @@ const performWorkflow = async (chapterId, seriesId, action) => {
   if (!rule) throw new AppError('Unknown workflow action', 400);
   if (!rule.from.includes(chapter.status))
     throw new AppError(`Cannot perform '${action}' from status '${chapter.status}'`, 400);
+
+  if (rule.to === 'published') {
+    const series = await seriesRepo.findById(seriesId);
+    if (series && series.status !== 'published') {
+      await seriesRepo.update(seriesId, { status: 'published', updated_at: new Date().toISOString() });
+    }
+  }
+
   return chaptersRepo.update(chapterId, { status: rule.to, updated_at: new Date().toISOString() });
 };
 
@@ -78,8 +108,7 @@ const copyFromChapter = async (seriesId, sourceChapterId, payload) => {
 
   const newChapter = await createChapter(seriesId, {
     title: payload.title || `${source.title} (copy)`,
-    description: source.description,
-    cover_image_url: source.cover_image_url,
+    thumbnail_image_url: source.thumbnail_image_url,
   });
 
   // copy pages if source has pages
diff --git a/src/modules/mangaka/mangakaImport.service.js b/src/modules/mangaka/mangakaImport.service.js
index ce6bd08..2e72926 100644
--- a/src/modules/mangaka/mangakaImport.service.js
+++ b/src/modules/mangaka/mangakaImport.service.js
@@ -18,7 +18,7 @@ const importChapter = async (userId, seriesId, payload) => {
 
   const { data: chapter, error: cErr } = await supabase
     .from('chapter')
-    .insert({ series_id: seriesId, title, description, cover_image_url, chapter_number: chapterNumber, status: 'draft' })
+    .insert({ series_id: seriesId, title, thumbnail_image_url: cover_image_url || null, chapter_number: chapterNumber, status: 'draft' })
     .select()
     .single();
   if (cErr) throw cErr;
diff --git a/src/modules/mangaka/mangakaManuscripts.service.js b/src/modules/mangaka/mangakaManuscripts.service.js
index d85e70b..a7de886 100644
--- a/src/modules/mangaka/mangakaManuscripts.service.js
+++ b/src/modules/mangaka/mangakaManuscripts.service.js
@@ -30,7 +30,7 @@ const getManuscriptById = async (manuscriptId, seriesId) => {
 };
 
 const createManuscript = async (seriesId, userId, payload) => {
-  return manuscriptsRepo.create({ ...payload, series_id: seriesId, created_by: userId, status: 'draft' });
+  return manuscriptsRepo.create({ ...payload, series_id: seriesId, mangaka_id: userId, status: 'draft' });
 };
 
 const updateManuscript = async (manuscriptId, seriesId, payload) => {
diff --git a/src/modules/mangaka/mangakaPageTasks.service.js b/src/modules/mangaka/mangakaPageTasks.service.js
index 1b3d9df..a16069f 100644
--- a/src/modules/mangaka/mangakaPageTasks.service.js
+++ b/src/modules/mangaka/mangakaPageTasks.service.js
@@ -23,7 +23,7 @@ const buildTaskQuery = (filters) => {
   let query = supabase
     .from("page_task")
     .select(
-      `*, users!fk_page_task_assistant(user_id, username, email), users!fk_page_task_assigned_by(user_id, username, email)`,
+      `*, assistant:users!fk_page_task_assistant(user_id, username, email), assigned_by:users!fk_page_task_assigned_by(user_id, username, email)`,
       { count: "exact" },
     );
   if (filters.pageId) query = query.eq("page_id", filters.pageId);
@@ -43,7 +43,7 @@ const listTasks = async (pageId, filters) => {
   let query = supabase
     .from("page_task")
     .select(
-      "*, users!fk_page_task_assistant(user_id,username,email), users!fk_page_task_assigned_by(user_id,username,email)",
+      "*, assistant:users!fk_page_task_assistant(user_id,username,email), assigned_by:users!fk_page_task_assigned_by(user_id,username,email)",
       { count: "exact" },
     )
     .eq("page_id", pageId);
@@ -63,7 +63,7 @@ const listAllSeriesTasks = async (seriesId, filters) => {
   let query = supabase
     .from("page_task")
     .select(
-      "*, page:page_id(chapter:chapter_id(series_id)), users!fk_page_task_assistant(user_id,username,email)",
+      "*, page:page_id(chapter:chapter_id(series_id)), assistant:users!fk_page_task_assistant(user_id,username,email)",
       { count: "exact" },
     );
   if (filters.status) query = query.eq("status", filters.status);
@@ -212,14 +212,22 @@ const requestRevision = async (
     updated_at: new Date().toISOString(),
   });
 
+  const { data: latestSub, error: subErr } = await supabase
+    .from('page_submission')
+    .select('submission_id')
+    .eq('task_id', taskId)
+    .order('submitted_at', { ascending: false })
+    .limit(1)
+    .maybeSingle();
+  if (subErr) throw subErr;
+  if (!latestSub) throw new AppError('No submission found for this task to revise', 400);
+
   // create feedback record
   await pageTaskFeedbacksRepo.create({
-    task_id: taskId,
+    submission_id: latestSub.submission_id,
     mangaka_id: requesterId,
     assistant_id: task.assistant_id,
-    feedback_content,
-    feedback_type: feedback_type || "revision_request",
-    status: "pending",
+    content: feedback_content,
   });
 
   if (task.assistant_id) {
diff --git a/src/modules/mangaka/mangakaSeries.routes.js b/src/modules/mangaka/mangakaSeries.routes.js
index 3066872..e0ec66f 100644
--- a/src/modules/mangaka/mangakaSeries.routes.js
+++ b/src/modules/mangaka/mangakaSeries.routes.js
@@ -7,8 +7,8 @@ const ctrl = require('./mangakaSeries.controller');
 
 const uuid = z.string().uuid();
 
-const createSchema = z.object({ body: z.object({ title: z.string().min(1).max(255), description: z.string().optional(), cover_image_url: z.string().url().optional(), genre: z.string().max(100).optional(), status: z.enum(SERIES_STATUS).default('draft') }) });
-const updateSchema = z.object({ params: z.object({ seriesId: uuid }), body: z.object({ title: z.string().min(1).max(255).optional(), description: z.string().optional(), cover_image_url: z.string().url().optional(), genre: z.string().max(100).optional() }) });
+const createSchema = z.object({ body: z.object({ title: z.string().min(1).max(255), description: z.string().optional(), cover_image_url: z.string().url().nullable().optional(), genre: z.string().max(100).optional(), status: z.enum(SERIES_STATUS).default('draft') }) });
+const updateSchema = z.object({ params: z.object({ seriesId: uuid }), body: z.object({ title: z.string().min(1).max(255).optional(), description: z.string().optional(), cover_image_url: z.string().url().nullable().optional(), genre: z.string().max(100).optional() }) });
 const statusSchema = z.object({ params: z.object({ seriesId: uuid }), body: z.object({ status: z.enum(SERIES_STATUS) }) });
 const idSchema = z.object({ params: z.object({ seriesId: uuid }) });
 const memberSchema = z.object({ params: z.object({ seriesId: uuid }), body: z.object({ user_id: uuid, role_in_series: z.string().min(1).max(50) }) });
@@ -21,7 +21,6 @@ router.get('/:seriesId', validate(idSchema), ctrl.getSeriesById);
 router.get('/:seriesId/detail', validate(idSchema), ctrl.getSeriesDetail);
 router.post('/', validate(createSchema), ctrl.createSeries);
 router.patch('/:seriesId', validate(updateSchema), ctrl.updateSeries);
-router.patch('/:seriesId/status', validate(statusSchema), ctrl.updateSeriesStatus);
 router.delete('/:seriesId', validate(idSchema), ctrl.deleteSeries);
 
 // Workflow
diff --git a/src/modules/mangaka/mangakaSeries.service.js b/src/modules/mangaka/mangakaSeries.service.js
index fcbbdb7..7a3a22b 100644
--- a/src/modules/mangaka/mangakaSeries.service.js
+++ b/src/modules/mangaka/mangakaSeries.service.js
@@ -2,6 +2,7 @@ const supabase = require('../../config/supabase');
 const seriesRepo = require('../series/series.repository');
 const seriesMembersRepo = require('../seriesMembers/seriesMembers.repository');
 const usersRepo = require('../users/users.repository');
+const reviewSessionsRepo = require('../reviewSessions/reviewSessions.repository');
 const AppError = require('../../utils/appError');
 
 const SERIES_STATUS = require('../../constants/status').SERIES_STATUS;
@@ -9,7 +10,7 @@ const SERIES_STATUS = require('../../constants/status').SERIES_STATUS;
 const WORKFLOW = {
   'submit-review': { from: ['draft', 'rejected'], to: 'pending_review' },
   publish: { from: ['approved'], to: 'published' },
-  republish: { from: ['hidden', 'archived', 'draft', 'approved'], to: 'published' },
+  republish: { from: ['hidden', 'archived'], to: 'published' },
   hide: { from: ['published'], to: 'hidden' },
   archive: { from: ['published', 'hidden', 'approved', 'draft'], to: 'archived' },
 };
@@ -26,7 +27,12 @@ const listMySeries = async (userId, filters) => {
   if (ids.length === 0) return { data: [], total: 0 };
 
   let query = supabase.from('series').select('*', { count: 'exact' }).in('series_id', ids);
-  if (filters.status) query = query.eq('status', filters.status);
+  // Lu├┤n loß║íi trß╗½ series ─æ├ú bß╗ï x├│a mß╗üm (soft delete)
+  if (filters.status && filters.status !== 'deleted') {
+    query = query.eq('status', filters.status);
+  } else if (!filters.status) {
+    query = query.neq('status', 'deleted');
+  }
   if (filters.keyword) query = query.ilike('title', `%${filters.keyword}%`);
   query = query.order('created_at', { ascending: false })
     .range(filters.offset, filters.offset + filters.limit - 1);
@@ -77,7 +83,13 @@ const performWorkflow = async (userId, seriesId, action) => {
   if (!rule.from.includes(series.status))
     throw new AppError(`Cannot perform '${action}' from status '${series.status}'`, 400);
 
-  return seriesRepo.update(seriesId, { status: rule.to, updated_at: new Date().toISOString() });
+  const updated = await seriesRepo.update(seriesId, { status: rule.to, updated_at: new Date().toISOString() });
+
+  // NOTE: Khi Mangaka nß╗Öp (submit-review), series chuyß╗ân sang pending_review.
+  // Tantou Editor sß║╜ duyß╗çt tr╞░ß╗¢c, sau ─æ├│ mß╗¢i submit l├¬n Hß╗Öi ─Éß╗ông (tß║ío review_session).
+  // Xem API: POST /api/editor/series/:seriesId/submit-to-board
+
+  return updated;
 };
 
 // soft delete
diff --git a/src/modules/manuscriptFiles/manuscriptFiles.controller.js b/src/modules/manuscriptFiles/manuscriptFiles.controller.js
index d60fcc6..335eb85 100644
--- a/src/modules/manuscriptFiles/manuscriptFiles.controller.js
+++ b/src/modules/manuscriptFiles/manuscriptFiles.controller.js
@@ -3,7 +3,7 @@ const { sendSuccess } = require('../../utils/response');
 
 const listFiles = async (req, res, next) => {
   try {
-    const manuscriptId = req.params.manuscriptId;
+    const manuscriptId = req.query.manuscriptId || req.params.manuscriptId;
     const data = await service.listFiles({ manuscriptId });
     return sendSuccess(res, 200, data, 'Success');
   } catch (error) { next(error); }
diff --git a/src/modules/manuscripts/manuscripts.controller.js b/src/modules/manuscripts/manuscripts.controller.js
index b5c783d..ef558b8 100644
--- a/src/modules/manuscripts/manuscripts.controller.js
+++ b/src/modules/manuscripts/manuscripts.controller.js
@@ -1,13 +1,15 @@
 const service = require('./manuscripts.service');
 const { sendSuccess } = require('../../utils/response');
 const { parsePagination, buildPaginationMeta } = require('../../utils/pagination');
+const supabase = require('../../config/supabase');
+const AppError = require('../../utils/appError');
 
 const listManuscripts = async (req, res, next) => {
   try {
     const { page, limit, offset } = parsePagination(req.query);
-    const userId = req.params.userId;
-    const seriesId = req.params.seriesId;
-    const chapterId = req.params.chapterId;
+    const userId = req.params.userId || req.query.userId || req.query.mangakaId;
+    const seriesId = req.params.seriesId || req.query.seriesId;
+    const chapterId = req.params.chapterId || req.query.chapterId;
     const { data, total } = await service.listManuscripts({ userId, seriesId, chapterId, offset, limit });
     return res.status(200).json({ success: true, message: 'Success', data, pagination: buildPaginationMeta(page, limit, total) });
   } catch (error) { next(error); }
@@ -22,6 +24,17 @@ const getManuscriptById = async (req, res, next) => {
 
 const createManuscript = async (req, res, next) => {
   try {
+    if (req.body.chapter_id) {
+      const { data: chapter, error: chErr } = await supabase
+        .from('chapter')
+        .select('status')
+        .eq('chapter_id', req.body.chapter_id)
+        .maybeSingle();
+      if (chErr) throw chErr;
+      if (chapter && (chapter.status === 'approved' || chapter.status === 'published')) {
+        return next(new AppError('Cannot submit manuscript for an approved or published chapter', 400));
+      }
+    }
     const data = await service.createManuscript(req.body);
     return sendSuccess(res, 201, data, 'Manuscript created');
   } catch (error) { next(error); }
diff --git a/src/modules/pageSubmissions/pageSubmissions.repository.js b/src/modules/pageSubmissions/pageSubmissions.repository.js
index 2c0dbb1..c054c1d 100644
--- a/src/modules/pageSubmissions/pageSubmissions.repository.js
+++ b/src/modules/pageSubmissions/pageSubmissions.repository.js
@@ -1,8 +1,21 @@
 const supabase = require('../../config/supabase');
 
 const SELECT = `*,
-  page:page_id(page_id, page_number, chapter_id),
-  task:task_id(task_id, task_type, status),
+  page:page_id(
+    page_id,
+    page_number,
+    chapter_id,
+    chapter:chapter_id(
+      chapter_id,
+      chapter_number,
+      title,
+      series:series_id(
+        series_id,
+        title
+      )
+    )
+  ),
+  task:task_id(task_id, task_type, status, deadline),
   assistant:users!fk_page_submission_assistant(user_id, username, name, avatar_url)`;
 
 const findAll = async ({ taskId, assistantId, submissionStatus, offset = 0, limit = 20 }) => {
diff --git a/src/modules/pageSubmissions/pageSubmissions.service.js b/src/modules/pageSubmissions/pageSubmissions.service.js
index 6355fd9..078cf11 100644
--- a/src/modules/pageSubmissions/pageSubmissions.service.js
+++ b/src/modules/pageSubmissions/pageSubmissions.service.js
@@ -76,8 +76,67 @@ const getSubmissionById = async (submissionId) => {
 };
 
 const listPendingSubmissions = async () => {
-  const { data } = await submissionsRepo.findAll({ submissionStatus: 'pending' });
-  return data;
+  const { data } = await submissionsRepo.findAll({ submissionStatus: 'pending', limit: 100 });
+  
+  const groups = {};
+  for (const sub of (data || [])) {
+    const chapterId = sub.page?.chapter_id || 'unknown-chapter';
+    if (!groups[chapterId]) {
+      const chapter = sub.page?.chapter || {};
+      const series = chapter.series || {};
+      groups[chapterId] = {
+        submission_id: chapterId,
+        series: {
+          title: series.title || 'ΓÇö'
+        },
+        chapter: {
+          title: chapter.title || `Ch.${chapter.chapter_number || '?'}`,
+          chapter_number: chapter.chapter_number || 0
+        },
+        submitted_at: sub.submitted_at,
+        deadline: sub.task?.deadline || null,
+        is_overdue: false,
+        overdue_pages_count: 0,
+        pages: []
+      };
+    }
+
+    const group = groups[chapterId];
+    const pageStatus = sub.submission_status || 'SUBMITTED';
+
+    const isPageOverdue = sub.task?.deadline ? new Date(sub.task.deadline) < new Date() && sub.submission_status === 'pending' : false;
+    if (isPageOverdue) {
+      group.is_overdue = true;
+      group.overdue_pages_count += 1;
+    }
+
+    if (sub.submitted_at && (!group.submitted_at || new Date(sub.submitted_at) > new Date(group.submitted_at))) {
+      group.submitted_at = sub.submitted_at;
+    }
+
+    if (sub.task?.deadline && (!group.deadline || new Date(sub.task.deadline) < new Date(group.deadline))) {
+      group.deadline = sub.task.deadline;
+    }
+
+    group.pages.push({
+      page_number: sub.page?.page_number || 0,
+      status: pageStatus,
+      assignee: {
+        username: sub.assistant?.username || 'ΓÇö'
+      },
+      created_at: sub.submitted_at,
+      deadline: sub.task?.deadline || null,
+      revision_count: sub.version_number ? Math.max(0, sub.version_number - 1) : 0,
+      notes: sub.submission_notes || ''
+    });
+  }
+
+  const result = Object.values(groups).map((group) => {
+    group.pages.sort((a, b) => a.page_number - b.page_number);
+    return group;
+  });
+
+  return result;
 };
 
 /**
diff --git a/src/modules/pageTaskFeedbacks/pageTaskFeedbacks.repository.js b/src/modules/pageTaskFeedbacks/pageTaskFeedbacks.repository.js
index 702c088..a52a314 100644
--- a/src/modules/pageTaskFeedbacks/pageTaskFeedbacks.repository.js
+++ b/src/modules/pageTaskFeedbacks/pageTaskFeedbacks.repository.js
@@ -1,7 +1,23 @@
 const supabase = require('../../config/supabase');
 
 const SELECT = `*,
-  submission:submission_id(submission_id, submission_status, version_number),
+  submission:submission_id(
+    submission_id,
+    submission_status,
+    version_number,
+    page:page_id(
+      page_id,
+      page_number,
+      chapter:chapter_id(
+        chapter_id,
+        title,
+        series:series_id(
+          series_id,
+          title
+        )
+      )
+    )
+  ),
   mangaka:users!fk_feedback_mangaka(user_id, username, name),
   assistant:users!fk_feedback_assistant(user_id, username, name)`;
 
@@ -31,6 +47,24 @@ const findBySubmissionId = async (submissionId) => {
   return data;
 };
 
+const findByTaskId = async (taskId) => {
+  const { data: submissions, error: subErr } = await supabase
+    .from('page_submission')
+    .select('submission_id')
+    .eq('task_id', taskId);
+  if (subErr) throw subErr;
+  const submissionIds = (submissions || []).map(s => s.submission_id);
+  if (submissionIds.length === 0) return [];
+  
+  const { data, error } = await supabase
+    .from('page_task_feedback')
+    .select(SELECT)
+    .in('submission_id', submissionIds)
+    .order('created_at', { ascending: true });
+  if (error) throw error;
+  return data;
+};
+
 const create = async (payload) => {
   const { data, error } = await supabase
     .from('page_task_feedback')
@@ -57,4 +91,4 @@ const deleteById = async (feedbackId) => {
   if (error) throw error;
 };
 
-module.exports = { findAll, findById, findBySubmissionId, create, update, deleteById };
+module.exports = { findAll, findById, findBySubmissionId, findByTaskId, create, update, deleteById };
diff --git a/src/modules/pages/pages.repository.js b/src/modules/pages/pages.repository.js
index 1b91bc4..8fb5915 100644
--- a/src/modules/pages/pages.repository.js
+++ b/src/modules/pages/pages.repository.js
@@ -6,7 +6,11 @@ const findAll = async ({ chapterId, status, offset, limit, sort, order }) => {
   let query = supabase.from('page').select('*', { count: 'exact' });
 
   if (chapterId) query = query.eq('chapter_id', chapterId);
-  if (status) query = query.eq('status', status);
+  if (status) {
+    query = query.eq('status', status);
+  } else {
+    query = query.neq('status', 'deleted');
+  }
 
   const sortCol = ALLOWED_SORT.includes(sort) ? sort : 'page_number';
   query = query.order(sortCol, { ascending: order !== 'desc' });
diff --git a/src/modules/reviewSessions/reviewSessions.repository.js b/src/modules/reviewSessions/reviewSessions.repository.js
index c2fdf67..19e6660 100644
--- a/src/modules/reviewSessions/reviewSessions.repository.js
+++ b/src/modules/reviewSessions/reviewSessions.repository.js
@@ -3,7 +3,7 @@ const supabase = require('../../config/supabase');
 const SELECT = `*,
   series:series_id(series_id, title),
   chapter:chapter_id(chapter_id, chapter_number, title),
-  created_by:users!fk_review_created_by(user_id, username, name)`;
+  creator:created_by_user_id(user_id, username, name)`;
 
 const findAll = async ({ seriesId, chapterId } = {}) => {
   let query = supabase.from('review_session').select(SELECT);
diff --git a/src/modules/reviewSessions/reviewSessions.service.js b/src/modules/reviewSessions/reviewSessions.service.js
index dbadc8c..acf8a17 100644
--- a/src/modules/reviewSessions/reviewSessions.service.js
+++ b/src/modules/reviewSessions/reviewSessions.service.js
@@ -37,16 +37,49 @@ const createSession = async (payload) => {
   return sessionsRepo.create(payload);
 };
 
+const handleAutoDecision = async (sessionId) => {
+  try {
+    const { processSessionResult, applyDecisionToChapter, applyDecisionToSeries } = require('../board/boardDecisions.service');
+    const result = await processSessionResult(sessionId);
+    
+    let decisionStr = '';
+    const upperDec = result.dominant_decision?.toUpperCase() || '';
+    if (upperDec === 'APPROVE' || upperDec === 'APPROVED') decisionStr = 'approved';
+    else if (upperDec === 'REJECT' || upperDec === 'REJECTED') decisionStr = 'rejected';
+    
+    if (decisionStr) {
+      const session = await sessionsRepo.findById(sessionId);
+      if (session) {
+        if (session.chapter_id) {
+          await applyDecisionToChapter(session.chapter_id, decisionStr, `Automatically ${decisionStr} based on board votes.`);
+        } else if (session.series_id) {
+          await applyDecisionToSeries(session.series_id, decisionStr, `Automatically ${decisionStr} based on board votes.`);
+        }
+      }
+    }
+  } catch (err) {
+    console.error(`Error auto-applying decision for session ${sessionId}:`, err);
+  }
+};
+
 const updateSession = async (sessionId, payload) => {
   const session = await sessionsRepo.findById(sessionId);
   if (!session) throw new AppError('Review session not found', 404);
-  return sessionsRepo.update(sessionId, payload);
+  const updated = await sessionsRepo.update(sessionId, payload);
+  if (['completed', 'finished'].includes(payload.status)) {
+    await handleAutoDecision(sessionId);
+  }
+  return updated;
 };
 
 const updateSessionStatus = async (sessionId, status) => {
   const session = await sessionsRepo.findById(sessionId);
   if (!session) throw new AppError('Review session not found', 404);
-  return sessionsRepo.update(sessionId, { status });
+  const updated = await sessionsRepo.update(sessionId, { status });
+  if (['completed', 'finished'].includes(status)) {
+    await handleAutoDecision(sessionId);
+  }
+  return updated;
 };
 
 const performWorkflow = async (sessionId, action) => {
@@ -60,7 +93,11 @@ const performWorkflow = async (sessionId, action) => {
   const update = { status: rule.to };
   if (rule.setStartedAt) update.started_at = new Date().toISOString();
   if (rule.setEndedAt) update.ended_at = new Date().toISOString();
-  return sessionsRepo.update(sessionId, update);
+  const updated = await sessionsRepo.update(sessionId, update);
+  if (['completed', 'finished'].includes(rule.to)) {
+    await handleAutoDecision(sessionId);
+  }
+  return updated;
 };
 
 const deleteSession = async (sessionId) => {
diff --git a/src/modules/series/series.validation.js b/src/modules/series/series.validation.js
index 8aebf99..44781f3 100644
--- a/src/modules/series/series.validation.js
+++ b/src/modules/series/series.validation.js
@@ -1,4 +1,4 @@
-∩╗┐const { z } = require('zod');
+const { z } = require('zod');
 const { SERIES_STATUS } = require('../../constants/status');
 
 // PostgreSQL accepts UUID-shaped values even when they do not encode an RFC version.
@@ -11,7 +11,7 @@ const createSeriesSchema = z.object({
   body: z.object({
     title: z.string().min(1).max(255),
     description: z.string().optional(),
-    cover_image_url: z.string().url().optional(),
+    cover_image_url: z.string().url().nullable().optional(),
     genre: z.string().max(100).optional(),
     status: z.enum(SERIES_STATUS).default('draft'),
   }),
@@ -22,7 +22,7 @@ const updateSeriesSchema = z.object({
   body: z.object({
     title: z.string().min(1).max(255).optional(),
     description: z.string().optional(),
-    cover_image_url: z.string().url().optional(),
+    cover_image_url: z.string().url().nullable().optional(),
     genre: z.string().max(100).optional(),
   }),
 });
